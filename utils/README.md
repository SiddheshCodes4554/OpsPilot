# Global Utilities Directory

This directory contains pure, framework-agnostic utility functions for the **OpsPilot AI** application.

## Structure
- Utilities here are pure functions (e.g. date formatting, currency formatting, math helpers, string manipulations).
- For React-specific helpers, consider putting them in `hooks/` or features if they are localized.
